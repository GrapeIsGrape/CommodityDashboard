# CommodityDashboard — Roadmap (PM blackboard)

This file is the **PM agent's living plan** — the shared "brain" the team reads and the PM writes. Sub-agents are stateless and forget everything between calls, so the *current* picture of what's done, what's next, and why lives **here on disk**, not in any agent's memory.

- **Authority:** `README.md` is the spec; `CLAUDE.md` is the working summary; this file tracks execution state. On conflict, README wins — flag the drift.
- **Who edits:** the **PM** updates this after each cycle (ticket done, phase advanced, new tickets surfaced). The BA/Dev/QA/Trader *read* it; they don't edit it.
- Keep it short and current. When it disagrees with the GitHub issues or the commits, the issues/commits are reality — reconcile and fix this file.

_Last reconciled: 2026-06-26 (**Phase 5 IN PROGRESS** — #19 Cushing + #23 scheduler + #24 run-log/heartbeat + **#25 /health stale flag** shipped. #23 (`etl/scheduler.py` + `config/scheduler.yaml`) is the first Phase-5 ticket and resolves the long-deferred Phase 2/3 scheduler loose end: ET-anchored slots (08:30 fred · 11:00 eia · 12:15 usda · 15:30 wk curve · 16:00 cftc · 16:20 wk close-batch) resolved against `America/New_York` (DST-automatic), in-process loop (Compose default) OR one-shot `--slot` (Railway/DSM crons) from the same image, a pure clock-injectable ET session-window guard as the UTC-cron backstop, per-source isolation + idempotency preserved, no migration/source/key (`tzdata` dep added). SecAudit✓ (one informational MEDIUM — boot-minute slot re-fire, harmless by idempotency) QA✓ (all 11 ACs) UAT✓ (Trader confirms the UTC-midnight trap is rejected in both DST halves and the panels will accrue). This turns on the clock so Panel D IV-rank / Panel C COT index / Panel B percentiles start accruing real numbers. **Phase-5 backlog flagged by #23/UAT: a run-log/heartbeat table for missed/failed-run observability** (overlaps health-checks/logging — see surfaced-not-filed below). HISTORY: **Phase 4 COMPLETE**: #13 Panel D, #14 Panel C, #15 Panel A, #16 Panel A polish, #18 Panel B, #20 prices-ETL, #21 macro sub-panel, and **#22 sentiment placeholder panel render** all shipped. #22 (`GET /panel/sentiment`) is the LAST Phase-4 render — reads `sentiment_articles`/`sentiment_scores` (empty in v1), three honest states (EMPTY "awaiting Writer-2" / UNAVAILABLE DB-down-or-pre-migration, visually distinct / forward-compatible POPULATED surfacing score AND model reasoning), NULL≠0 score discipline, `safe_href` http(s)-only link allow-list, no migration — SecAudit✓ (one MEDIUM `javascript:`/`data:` href gap folded into the same commit as the `safe_href` allow-list) QA✓ (all 8 ACs + the hardening) UAT✓ (Trader confirms the EMPTY/UNAVAILABLE distinction and reasoning-next-to-score are right for a placeholder). Trader UAT backlog (out-of-scope nice-to-haves, wait for real Writer-2 data): per-commodity sentiment rollup; cross-link sentiment into Panel D as soft context. Open: #19 (Cushing tank-bottom, later). **Next: Phase 5 — Polish & deploy** (deploy Compose stack, release calendar, health checks/logging, redeploy docs), or the still-deferred scheduler wiring.)_

---

## Phase pointer

**Current position: Phase 5 IN PROGRESS (Polish & deploy).** Phase 4 complete. **Scheduler wired (#23) + run-log/heartbeat (#24) + /health stale flag (#25) + manual ETL trigger (#29) done.** Phase 2 complete (FRED #3, EIA #4, USDA #6, CFTC #7). Phase 3 ETL sources complete (#9 IV, #10 vol indices, #11 curve, #12 anchoring). **Phase 4 shipped (all renders):** **Panel D (Volatility) → `GET /panel/d` (#13)** — Jinja2 foundation (base layout + reusable panel shell) + read-only `iv_metrics` view; conjunctive rich-highlight (`iv_rank≥0.70 AND iv_rv_spread>0`); cold-start `— (N/20)`; daily staleness; GVZ/OVX strip. **Panel C (Positioning & Flow) → `GET /panel/c` (#14)** — read-only `cot` + `curve_shape`; headline 3y COT index (156-wk net-spec percentile) + loud ≥80/≤20 crowding flags with directional seller inference; cold-start `— (accruing M/156)`; separate 5-card energy curve strip (loud backwardation, NULL `— (no curve)` ≠ flat); COT-correct weekly Tue→Fri staleness + holiday grace. Both no migration, both DB-error-isolated (no 500). **Panel A (Macro / Cross-Asset) → `GET /panel/a` (#15)** — read-only `macro_metrics`; five-bucket grouping (USD / real-rates+breakevens / realized-inflation / growth+labor / risk-regime) config-driven from new `freq`/`group` keys on `fred_series.yaml`; level+change every row (inflation indices headline YoY% from stored 12-mo-prior row); DFII10 shown directly (never recomputed); release-aware monthly/quarterly staleness (publication-lag model — freshest print never STALE); VIX as cross-asset regime, no IV-rank, separated from Panel D's GVZ/OVX; neutral coloring, no option-action language. No migration, DB-error-isolated.
**Next up: rest of Phase 5 — Polish & deploy.** All Phase-4 renders shipped, scheduler wired (#23), run-log/heartbeat (#24), stale flag (#25), release calendar (#27), manual ETL trigger (#29) done. Remaining Phase-5 program: **(a) deploy the Compose stack** (confirm the scheduler actually fires on the deploy target — Railway cron or DSM, per README §9); **(b) redeploy docs.** Also queued: #28 (NFP affected field broadened to include energy). Candidate next ticket: **deploy + verify the scheduler on Railway** (ops — the whole stack is now observable end-to-end so a real deploy + fire confirmation is the highest-value next step).

Open backlog: ~~**#8**~~ DONE · ~~**#12**~~ DONE · ~~**#13** (foundation + Panel D)~~ **DONE** · ~~**#14** (Panel C)~~ **DONE** · ~~**#15** (Panel A)~~ **DONE** · ~~**#16** (Panel A polish)~~ **DONE** · ~~**#18** (Panel B fundamentals/inventory)~~ **DONE** · ~~**#20** (prices-ETL writer + migration 0003)~~ **DONE** · ~~**#21** (macro-context sub-panel render → `GET /panel/macro`)~~ **DONE** · ~~**#22** (sentiment placeholder panel → `GET /panel/sentiment` — completes Phase 4)~~ **DONE**. ~~**#19** (Panel B Cushing tank-bottom proximity)~~ **DONE (2026-06-21, `a22116a`)** — Cushing-only config-driven "% of operable shell capacity" + near-bottom/low badge, thresholds in a `capacity:` block on the Cushing entry in `config/eia_series.yaml`, neutral vol-bid CONTEXT, honest NULL when unconfigured, no migration/source/env. SecAudit✓ QA✓ UAT✓. · ~~**#23** (ETL scheduler wiring)~~ **DONE (2026-06-22, `cae7a8a`)** — `etl/scheduler.py` + `config/scheduler.yaml`, ET-anchored slots resolved against `America/New_York`, in-process loop + one-shot `--slot` from one image, pure ET session-window guard (UTC-cron backstop), per-source isolation + idempotency preserved, no migration/source/key (`tzdata` dep, `ETL_TZ` env). SecAudit✓ (1 informational MEDIUM, harmless) QA✓ (11/11 ACs) UAT✓. · ~~**#24** (ETL run-log / heartbeat + /health surfacing)~~ **DONE (2026-06-25, `d6d86ff`)** — `etl/run_log.py` best-effort upsert into `etl_run_log` (migration `0004`, UNIQUE `(slot, source, run_date)`); `dispatch_slot` writes `success`/`failure`/`skipped` per source; `/health` gains `etl` key with problems-first reconciled list incl. `never_ran` for configured-but-unrun sources; secrets redacted in `detail` (`key=value` + DSN userinfo forms). SecAudit✓ (HIGH DSN-leak hardened + MEDIUM benign-prose over-matching fixed) QA✓ UAT✓ (NR-1 gap closed). · ~~**#25** (per-source stale flag on /health ETL summary)~~ **DONE (2026-06-25, `514d3cb`)** — `is_etl_source_stale()` pure clock-injectable helper; slot-time-aware weekday model (Monday 08:00 before 16:20 slot → `stale: false` for Friday run); `_DAILY_GRACE_DAYS=2` for release-driven sources; holiday-aware via `panel_a._is_trading_session`; `never_ran` → `stale: null`; malformed slot_time_str → graceful fallback; no migration. SecAudit✓ (MEDIUM unguarded split fixed) QA✓ UAT✓ (NR-1 fixed, NR-2 filed separately as low-priority). · ~~**#27** (economic-release calendar strip on index page)~~ **DONE (2026-06-26)** — `common/release_calendar.py` stdlib-only pure module: `_FEDERAL_HOLIDAYS` (2025–2027), `compute_eia_petroleum_events` (Wed 10:30 ET + holiday shift + Thanksgiving-week EIA-early note), `compute_eia_natgas_events` (Thu 10:30 ET), `compute_nfp_events` (first-Friday 08:30 ET), `compute_cot_events` (Fri 15:30 ET, de-emphasized), `load_yaml_events` (WASDE/FOMC/CPI from `config/release_calendar.yaml`, per-entry error isolation), `upcoming_events(now_et, cal_config, days=7)`. `dashboard/templates/index.html` 7-day strip: TODAY/TOMORROW labels, elapsed strikethrough, holiday-delayed badge, COT de-emphasis, year-boundary unconfigured note, empty state. `common/config.py` `load_release_calendar()` (FileNotFoundError → `{}`, optional `RELEASE_CALENDAR_CONFIG` env override). `RELEASE_CALENDAR_CONFIG=` in `.env.example`. tzdata dep in `dashboard/requirements.txt`. No migration, no new DB table. SecAudit✓ (3 HIGH + 1 MEDIUM all fixed) QA✓ (35/35 tests) UAT✓ (UAT NR-1 blocker fixed: WASDE Saturday date corrected to Friday 2026-07-10 + no-weekend YAML guard test added). Filed **#28** (NFP affected field broadened to include energy — chore, low-priority). · ~~**#29** (manual ETL trigger button on /health)~~ **DONE (2026-06-26)** — `/health` converts from JSON to HTML (`health.html` template); `POST /health/trigger` inserts into new `etl_manual_trigger` table (migration `0006`); ETL scheduler `_check_manual_trigger()` polls at top of each tick + dispatches all slots; mark-before-dispatch prevents double-fire; 10-min rate limit; `OperationalError` → 503 JSON, `ProgrammingError` → redirect pre-migration banner; GVZ/OVX `atm_iv` unit fix via migration `0005` (÷100 for decimal-fraction convention). SecAudit✓ (HIGH double-fire fixed: mark-before-dispatch) QA✓ (3 gap-fixes folded in) UAT✓. **Next: deploy + verify scheduler on Railway** (ops).

The PM loop crosses phase boundaries by default.

---

## Phase status

| Phase | Scope | Status |
|---|---|---|
| 0 — Volatility data spike | de-risk IV via yfinance (no IBKR/paid feed for v1) | ✅ done 2026-06-14 |
| 1 — Foundation & schema | Compose scaffold (#1), data tables migration `0002` (#2) | ✅ done |
| 2 — Free-data ETL | FRED #3, EIA #4, USDA #6, CFTC #7 (idempotent, backfilled); **scheduler deferred** | ✅ done (sources) |
| 3 — Volatility & positioning ETL | IV → `iv_metrics` (#9 ✅), vol indices GVZ/OVX (#10 ✅), `curve_shape` (#11 ✅) | ✅ done (sources; scheduler deferred) |
| 4 — Dashboard (FastAPI) | four panels + macro sub-panel + empty sentiment panel; surface COT extremes, rich IV, backwardation flags | ✅ done — Panel D (#13) + Panel C (#14) + Panel A (#15 + #16 polish) + Panel B (#18) + macro-context sub-panel (#21) + sentiment placeholder (#22) |
| 5 — Polish & deploy | deploy Compose stack, release calendar, health checks/logging, redeploy docs | 🟡 in progress — **#23 ✅ + #24 ✅ + #25 ✅ + #27 ✅ + #29 ✅**; remaining: deploy + verify on target, redeploy docs |

---

## Candidate next tickets (Phase 3)

The PM refines these each cycle against live issues/commits before filing. Smallest-coherent-step first, matching the Phase-2 ETL pattern (config-driven, append-only, idempotent, per-source isolation, swappable interface).

1. ~~IV ETL via yfinance → `iv_metrics`~~ — **DONE (#9, `baf0ec6`)**, behind swappable `get_iv()`.
2. ~~Vol indices GVZ + OVX → `iv_metrics`~~ — **DONE (#10)**: `etl/sources/vol_indices.py`, swappable `IndexHistoryProvider`, ~3y backfill, trailing-365d rank shared with #9, VIX excluded (`ingest: false`), no migration.
3. ~~Curve shape (contango/backwardation) → `curve_shape`~~ — **DONE (#11)**: energy-only (CL/BZ/NG/RB/HO), front-vs-deferred annualized `slope_pct` + `structure` flag (0.5% deadband), swappable `CurveProvider`, honest-NULL, no migration. ETF-roll proxies rejected; metals/grains/softs + base-metals flagged-not-faked. Refinement tracked in #12.
4. **Scheduler wiring** (deferred from Phase 2/3) — swappable Compose-cron / Railway-cron / DSM Task Scheduler. The remaining Phase 3 loose end; may land before or alongside Phase 4.

Also open (non-Phase-3): **#8** add `schema_version` to dashboard `/health` — migration-observability tooling.

## Known deferrals / flagged-not-faked
- Metals warehouse stocks — not on EIA API (paid feed/scraping).
- WASDE supply/demand balance sheet — report files, not a queryable API.
- ISM PMIs (licensed) and ICE DXY — excluded; DXY proxied by FRED `DTWEXBGS`.
- Base metals COT (ALI/ZNC/NICKEL) — LME, no CFTC legacy report.

## Recently shipped (out of phase)
- ~~**#17 — dashboard crash-loop on Railway (`ModuleNotFoundError: No module named 'etl'`)**~~ — **DONE (2026-06-18, `468f320`)**: `panel_d.py` imported `_MIN_HISTORY_OBS` from `etl.sources.iv`, but the dashboard image ships no `etl/`. Relocated the constant to new stdlib-only `common/constants.py` (single source of truth); `iv.py` re-exports it, `panel_d.py` imports from `common.constants`; static-scan regression guard (`test_no_dashboard_module_imports_etl`) forbids any future dashboard→etl import. No migration/dep/env.

## Surfaced-but-not-yet-filed (PM appends here as Dev blockers / Trader UAT findings arrive)
- ~~**Run-log / heartbeat table + health-checks**~~ — **FILED and DONE as #24** (`d6d86ff`, 2026-06-25). See #24 in the backlog above.
- **`stale: null` / never_ran JSON disambiguation** (Trader UAT NR-2 on #25, LOW priority) — in JSON, `stale: null` (never_ran) and `stale: false` (fresh) are both falsy; a consumer script doing `if not entry["stale"]` silently treats never_ran as fresh. Since `last_status: "never_ran"` is already in the same row, this is a documentation/UX concern, not a correctness bug. Fix options: omit `stale` key from never_ran rows (missing ≠ false), or document that `last_status` must be checked first. Not worth blocking #25; file as a Phase-5 polish ticket if a monitoring script is ever written against `/health`.
- **Half-day early-close (13:00 ET) honest-NULL on the 16:20 batch** (Trader UAT on #23, accepted/non-blocking) — on a half-day the 16:20 close-batch still passes the session-window guard and may hand `iv.py` a dead chain → one honest-NULL snapshot that day. Acceptable for v1 (honest-NULL absorbs it, one missed snapshot). A half-day calendar was explicitly out of scope for #23; revisit only if missed half-day snapshots prove annoying.
- **In-process scheduler loop is minute-match** (Trader UAT on #23, latent-only) — `run_forever` dedupes per calendar minute; if a source `run()` overran a full minute it could skip an adjacent-minute slot. No two canonical slots are adjacent minutes, so a non-issue today — only a trap if someone later packs slots into consecutive minutes. Worth a one-line code comment someday, not a ticket.
- **Evaluate additional commodity vol indices for `iv_metrics`** (Trader UAT on #10) — esp. `^VXSLV` (silver/SLV); GVZ/OVX are the only CBOE *commodity* vol indices with durable Yahoo history, VXSLV has been intermittently discontinued. Follow-up should *gate on the index still publishing* (flag-not-fake), reusing the #10 config `indices` list (zero schema change). Not Phase-3-blocking.
- **Panel-D staleness flag for vol-index rows** (Trader UAT on #10) — render `snapshot_date` and flag when the latest GVZ/OVX close is stale (> N days, holiday/halt gap). Belongs with the Phase 4 dashboard ticket, not the ETL.
- ~~**#12 — curve-shape deferred-gap anchoring**~~ — **DONE**: config-driven per-contract `front_lead_months`/`roll_day` realized-front expiry rules; deferred ticker + slope denominator share one realized-front anchor so magnitude is correctly scaled near rolls. Residual ±1-day roll-boundary → honest-NULL.
- **Panel-C curve magnitude caveat near rolls** (Trader UAT on #11) — when Panel C renders, caveat the `slope_pct` magnitude in roll week (tie to #12); the `structure` sign flag needs no caveat. Phase 4 rendering concern.
- **Schedule the IV / vol-index / curve snapshots during/after the relevant session** (Trader UAT on #9/#10/#11) — off-hours/stale legs store NULL; the deferred scheduler ticket should run these when fresh settles exist.
- **Panel D secondary sort by `iv_rv_spread DESC during cold-start accrual** (Trader UAT on #13, nice-to-have) — while every per-name `iv_rank` is NULL (~20-session warm-up), `iv_rank DESC NULLS LAST` collapses all names into one bucket so the table is unordered; a secondary sort on IV−RV would make it useful sooner. Polish, not blocking — the GVZ/OVX strip carries the decision in that window.
- **Panel D `— (N/20)` countdown is ~1 session imprecise** (Trader UAT on #13, cosmetic) — the panel counts non-null `atm_iv` incl. today while `iv.py` lights rank at 19 priors + today, so the displayed ETA can be off by one session. No effect on any tradeable number; fix only if it confuses.
- **US market-holiday table in Panel D staleness is fixed 2025–2026** (QA/UAT note on #13) — `panel_d.py` `_US_MARKET_HOLIDAYS` must be extended as years roll forward; an unrecorded future holiday risks at most one false STALE badge, never bad data. Same applies to Panel C's `_US_FEDERAL_HOLIDAYS` table (#14).
- **Panel D × Panel C cross-reference: rich-IV ∧ COT-extreme = highest-conviction "sell the opposite side"** (Trader consult+UAT on #14, nice-to-have) — a name that is both rich IV (Panel D) and at a crowded-spec extreme (Panel C) is the strongest sell-the-other-side setup; surface a cross-panel flag/note once both panels exist. Deferred from #14, worth a dedicated ticket.
- **Panel C `slope_pct` percentile vs own 1y history + seasonality self-calibration** (Trader consult on #14, future ticket) — make the curve backwardation flag self-calibrating like the COT index (so seasonally-normal energy backwardation isn't read as a signal); needs a per-name seasonal/own-history model. Separate ticket.
- **Panel C Friday-morning intraday staleness over-eagerness** (QA note on #14, cosmetic) — `expected_cot_report_date` treats the whole Friday as past the ~15:30 ET release, so a Friday-pre-release read could badge the still-current row STALE for a few hours; day-granularity is acceptable for v1, holiday grace absorbs most. Fix only if intraday staleness matters.
- **Panel A nice-to-have polish** (Trader UAT on #15, non-blocking) — (a) render DTWEXBGS ~1m/~3m change as a **percent** not index points (a broad-index move reads naturally in %, maps to commodity headwind/tailwind), keeping rate series in pp and CPI-style indices absolute; (b) optional **VIX calm/normal/stressed band** tag from level (descriptive context, no option verbs, AC#8-clean); (c) USD-trend / real-rate-trend one-line context clauses with the commodity-linkage gloss. **Being filed as a single polish follow-up ticket.** Panel A's blocking UAT bug (release-aware staleness) was fixed in-ticket before close.
- ~~**`prices` table has no ETL writer**~~ **RESOLVED (#20)** — prices-ETL now writes TLT/VTI/QQQ (raw close + total-return adj_close, migration 0003). The macro sub-panel render can proceed. Commodity-universe daily prices remain deferred (redundant with curve_shape/iv_metrics, no v1 consumer) — add when a consumer needs it.
- **Verify real `inventories` backfill depth** (Trader UAT NR-3 on #18, ops check not code) — Panel B's weekly tight/loose percentile only leaves the `— (accruing M/N)` state once ~9 seasonal comparables (~3 prior same-season years) are stored; the EIA/USDA config says `observation_start: 2005` but the percentile column is only useful in production if the ETL actually backfilled that deep. Confirm stored history depth per series; if shallow, the percentile column reads accruing and only the always-honest WoW build/draw + Δ-vs-year-ago carry the panel (by design). No code change — a data/ops check (run it when the scheduler/backfill ETL is live).
- **Sentiment panel: per-commodity rollup + Panel-D cross-link** (Trader UAT on #22, backlog — wait for real Writer-2 data) — once Writer 2 actually populates `sentiment_*`: (a) a per-commodity net-sentiment rollup so a trader can glance "net sentiment on CL" without scanning every article; (b) cross-link the sentiment read into Panel D as soft context next to the IV-rich flag. Both explicitly out of scope for #22 (placeholder); design once there's real data volume.
- **Manual trigger off-hours session-window note** (Trader UAT NR-b on #29, LOW priority) — when the user clicks "Run all ETL now" outside 15:25–16:35 ET, session-guarded sources (iv/vol_indices/prices/curve_shape) are skipped. The success banner currently says nothing about this. A low-priority follow-up could add a note on the banner when `now_et` is outside the window, so the user isn't confused that the market-data sources didn't fire. Not a correctness issue (honest-NULL behavior is documented elsewhere); a UX polish item.
- **Panel B same-week-of-year seasonal band + 5-yr envelope** (deferred from #18, flag-not-fake) — the polished EIA-style seasonal min/max band is the percentile column's end-state; gated on ≥5 yrs stored weekly history per series. Its own later ticket. Also deferred from #18: grain stocks-to-use (needs WASDE use denominator), and the consensus/expectations "surprise" (paid/scraped feed).
