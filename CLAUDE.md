# CLAUDE.md — CommodityDashboard

Project context for Claude Code. Read this before working on any ticket or change.
The authoritative spec is [README.md](README.md); this file is the working summary that grounds the skills. When they conflict, the README wins — and flag the drift.

---

## 1. What this is

A **personal, single-user** dashboard that aggregates the market data driving the commodities the owner sells options on, to spot **when premium is rich** and **which underlyings are worth selling**.

- **Read-only monitoring tool.** It never places trades or moves money — it reads market data, stores it, and displays it. Any change that writes orders or touches a broker is out of scope.
- **Single user.** There is no auth, no multi-tenancy, no per-user data scoping. Do not add login or `user_id` columns.

### 1 DB, 2 writers
One shared Postgres, two independent writers to **separate tables**:
- **Writer 1 (this project, now):** ETL scripts pull pure market data on a schedule.
- **Writer 2 (separate project, later):** scheduled LLM news-sentiment task writing to its own `sentiment_*` tables. Not built here — only leave room (placeholder schema + empty dashboard panel).

---

## 2. Tech stack

- **Language:** Python
- **Dashboard/API:** FastAPI (server-rendered, read-only) — **Jinja2** server-side templating (`dashboard/templates/`), no SPA/client-side fetch
- **ETL:** standalone Python scripts/modules, one module per data source
- **DB:** PostgreSQL (single shared database)
- **Packaging:** Docker Compose — services `postgres`, `etl`, `dashboard`
- **Deploy targets:** Railway **or** Synology NAS, no code changes between them
- **Migrations:** Alembic (decided in #1) — config-as-code in `migrations/`, DB URL built from env in `env.py`
- **Scheduler:** swappable (Compose cron / Railway cron / DSM Task Scheduler) — not yet chosen

External data sources: **FRED** (macro), **EIA** (energy inventories), **USDA NASS/WASDE** (grains), **CFTC** (COT positioning), **yfinance** (prices, option-chain IV, `^VIX`/`^GVZ`/`^OVX`). All free. Flag anything needing a paid feed or scraping (metals warehouse stocks, multi-expiry futures curves).

---

## 3. Architecture decisions (already decided — follow them)

- **Portability:** all environment-specific config in `.env` / env vars. Never hardcode hosts, ports, credentials, or API keys.
- **Separate tables per writer:** sentiment tables must be addable later without touching the data tables.
- **Store history, not just latest:** every table is time-stamped and append-friendly. **Never overwrite — insert new dated rows.**
- **Idempotent ETL:** re-running a job for the same date must not create duplicates — upsert on natural keys like `(symbol, metric, date)`.
- **Secrets via env only:** keys in `.env` (git-ignored); a committed `.env.example` lists required keys.
- **Migrations for every schema change.**
- **Config-driven symbol list:** the underlyings live in a YAML/JSON config, not hardcoded — add/remove without code changes. Config also holds the **commodity → optionable-ETF-proxy** mapping (IV comes via GLD/SLV/USO/UNG…, not futures symbols).

**Formatting conventions:** dates `YYYY-MM-DD` · currency USD · display numbers with thousands separators.

---

## 4. Engineering principles (apply throughout)

- **Append-only, time-stamped, idempotent ETL** — never overwrite, no duplicate rows on re-run.
- **Config-driven** — no hardcoded hosts, keys, or symbols.
- **Secrets in env vars** — `.env.example` committed, `.env` git-ignored.
- **Migrations for every schema change.**
- **Each ETL source is its own module** with its own error handling and logging — one failing source must not break the others or the dashboard.
- **Prefer free public APIs**; clearly flag any source needing a paid feed or scraping.
- **The dashboard is read-only** — it never executes trades or moves money.
- Keep the IV source behind a clean swappable interface (e.g. `get_iv(symbol)`) so IBKR can replace yfinance later without touching the rest of the app.

---

## 5. Dashboard panels & key tables

The dashboard is organized into four panels plus a macro-context sub-panel and an (empty) sentiment panel.

| Panel | Content | Primary source | Table(s) |
|---|---|---|---|
| **A — Macro / Cross-Asset** | DXY, rates/real yields, CPI/PCE/PPI/breakevens, employment, GDP, PMIs, VIX — **rendered (#15)** at `GET /panel/a` | FRED | `macro_metrics` |
| **B — Fundamentals / Inventory** | EIA petroleum & nat-gas storage, USDA WASDE/crop progress, production, demand proxies — **rendered (#18)** at `GET /panel/b` | EIA, USDA | `inventories` |
| **C — Positioning & Flow** | CFTC COT (flag specs crowded long/short), ETF holdings, futures curve shape (contango/backwardation) — **rendered (#14)** at `GET /panel/c` | CFTC | `cot`, `curve_shape` |
| **D — Volatility** *(where decisions live)* | IV + IV rank/percentile per underlying, OVX/GVZ/VIX, realized vol, IV−RV spread, seasonality — **rendered (#13)** at `GET /panel/d` | yfinance | `iv_metrics` |
| Macro-context sub-panel | TLT, VTI, QQQ — context, **not** commodities; **ETL writer done (#20)** → `prices` (raw `close` + total-return `adj_close`), panel render still pending | yfinance | `prices` |
| Sentiment (placeholder) | Empty until Writer 2 exists; store headlines, URLs, timestamps **and model reasoning** | — | `sentiment_*` |

Daily futures/spot prices → `prices`. An economic-release calendar anchors panels to dates.

**Dashboard highlights to surface:** COT extremes, rich IV (high IV rank), backwardation flags.

### Scope — commodities (v1)
Precious metals (GC/GLD, SI/SLV, PL, PA), base metals (HG, Aluminum, Zinc, Nickel), energy (CL, BZ, NG, RB, HO), grains/oilseeds (ZC, ZS, ZM, ZL, ZW/KE/MW, ZR, ZO), softs (KC, SB, CC, CT, OJ, LBR), livestock (LE, GF, HE). Macro context: TLT, VTI, QQQ. The canonical list lives in the symbol config, not here.

---

## 6. Build status & phased plan

**Stop after each phase for review.** Current position: **Phase 4 IN PROGRESS — all four data panels rendered: Panel D (Volatility, #13), Panel C (Positioning & Flow, #14), Panel A (Macro / Cross-Asset, #15 + polish #16), Panel B (Fundamentals / Inventory, #18). Prices ETL → `prices` done (#20, yfinance TLT/VTI/QQQ, raw `close` + total-return `adj_close`; migration `0003` adds `adj_close`) — this UNBLOCKS the macro-context sub-panel (render still pending). Phase 3 ETL sources COMPLETE — IV → `iv_metrics` (#9), vol indices GVZ/OVX → `iv_metrics` (#10), curve shape → `curve_shape` (#11) all done (VIX intentionally excluded — sourced from FRED `VIXCLS` → `macro_metrics`). Scheduler wiring still deferred (Phase 2/3). Phase 2 free-data ETL complete (FRED #3, EIA #4, USDA #6, CFTC #7). `/health` now also reports `schema_version` (current Alembic revision) for migration observability (#8). Next: Phase 4 macro-context sub-panel render (now unblocked by #20) + sentiment placeholder, or the deferred scheduler.**

- **Phase 0 — Volatility data spike — ✅ DONE (2026-06-14).** De-risked: **yfinance** delivers option-chain IV + vol indices + price history, **no IBKR or paid feed needed for v1**. Consequences: IV via optionable ETF proxies (not futures symbols); IV rank/percentile must be accrued from our own daily snapshots (Yahoo gives no IV history); keep the source behind a swappable interface. Throwaway proof: [spike_iv.py](spike_iv.py).
- **Phase 1 — Foundation & schema — ✅ DONE.** Scaffold (#1, 2026-06-14): Docker Compose (`postgres`/`etl`/`dashboard`), `.env.example`, symbol config, Alembic + empty `0001_baseline`, FastAPI boot page + `/health`; deployed on Railway. Schema (#2, 2026-06-15): migration `0002_data_tables` creates `prices`, `macro_metrics`, `inventories`, `cot`, `iv_metrics`, `curve_shape` — each time-stamped with a named natural-key UNIQUE constraint (idempotent upsert) and a `(key, date DESC)` index. Placeholder `sentiment_*` realised as two tables: `sentiment_articles` (raw inputs) + `sentiment_scores` (model score + reasoning).
- **Phase 2 — Free-data ETL — ✅ DONE (sources; scheduler deferred):** FRED, EIA, USDA, CFTC. Idempotent, backfilled. **FRED done (#3, 2026-06-15):** `etl/sources/fred.py` + `config/fred_series.yaml` + `load_fred_series()` — config-driven series list (Panel A → `macro_metrics`), idempotent upsert on `(series_id, date)`, incremental + revision-lookback start (backfill from `observation_start` on first run), per-series error isolation, env-only `FRED_API_KEY`, `"."` sentinel → NULL. Manual run (`python -m etl.sources.fred`); scheduler still deferred. ISM PMIs (licensed) and ICE DXY excluded — DXY proxied by `DTWEXBGS`. **EIA done (#4, 2026-06-16):** `etl/sources/eia.py` + `config/eia_series.yaml` + `load_eia_series()` — config-driven series list (Panel B → `inventories`), idempotent upsert on `(source, series_id, date)`, year-granular incremental start + backfill (safe across weekly/monthly period formats), offset/length pagination so long backfills aren't truncated, per-series error isolation, env-only `EIA_API_KEY` (redacted from request-failure logs), null/blank → NULL. Pulls via the v2 `seriesid` endpoint (chosen over route+facets); manual run (`python -m etl.sources.eia`). Metals warehouse stocks flagged as not on the EIA API (paid feed/scraping), not faked. **USDA done (#6, 2026-06-16):** `etl/sources/usda.py` + `config/usda_series.yaml` + `load_usda_series()` — config-driven query list (Panel B grains → `inventories` with `source='USDA'`, reusing the existing `(source, series_id, date)` key so **no migration**), idempotent upsert, year-granular incremental start + backfill via `year__GE`, per-series error isolation, env-only `USDA_NASS_API_KEY` (redacted from request-failure logs, the #5 pattern), NASS sentinels (`(D)`/`(NA)`/…) → NULL + thousands-separator stripping, per-row date from `week_ending` or `year`+`reference_period_desc`. Pulls via the free NASS QuickStats API; manual run (`python -m etl.sources.usda`). WASDE supply/demand balance sheet flagged as report files (not a queryable API), deferred — not faked. **CFTC done (#7, 2026-06-16):** `etl/sources/cftc.py` + `config/cftc_markets.yaml` + `load_cftc_markets()` — config-driven symbol → CFTC `cftc_contract_market_code` map (Panel C → `cot`), idempotent upsert on `(symbol, report_date)`, incremental + revision-lookback start (backfill from `observation_start` on first run), `$limit`/`$offset` pagination, per-market error isolation. Targets the **Legacy futures-only** report (dataset `6dca-aqww`) whose comm/non-comm split maps onto the `cot` columns; free Socrata API needs no key, optional `CFTC_APP_TOKEN` sent as `X-App-Token` header (raises rate limits only). All 28 contract codes were verified against the live API; base metals (ALI/ZNC/NICKEL) omitted (LME, no CFTC legacy report), not faked. Manual run (`python -m etl.sources.cftc`).
- **Phase 3 — Volatility & positioning ETL — ✅ sources DONE (scheduler deferred):** wire IV → `iv_metrics`, add curve shape and OVX/GVZ/VIX. **IV done (#9, 2026-06-16):** `etl/sources/iv.py` — daily vol snapshot per underlying → `iv_metrics`, idempotent upsert on `(symbol, snapshot_date)`. `atm_iv` from the underlying's optionable ETF proxy chain (`config/symbols.yaml` `iv_proxy`; null-proxy underlyings skipped), `rv_30` annualized realized vol from proxy price history, `iv_rv_spread` derived, `iv_rank`/`iv_percentile` accrued from our own stored `atm_iv` history (NULL until `_MIN_HISTORY_OBS=20` snapshots). Vol source behind a **swappable** `get_iv()` / `IVProvider` / `set_provider()` (yfinance is the only import site, so IBKR can replace it — CLAUDE.md §4). Honest NULL, not fake IV: only contracts with a live two-sided market (`bid>0`) are trusted + a plausibility floor + nearest-ATM-strike restriction, so off-hours/stale chains record `atm_iv=NULL` (schedule the snapshot during/after the option session). New dep `yfinance==0.2.66` (no key). Pure vol math is network-free/unit-tested; manual run (`python -m etl.sources.iv`). **Vol indices done (#10, 2026-06-16):** `etl/sources/vol_indices.py` — daily ingest of CBOE **GVZ** (gold/GLD) + **OVX** (WTI/CL) volatility indices → `iv_metrics` as rows `symbol='GVZ'`/`'OVX'` (caret stripped), index level in `atm_iv`, `source='yfinance'`, `rv_30`/`iv_rv_spread` NULL (no underlying price series). Reuses #9's `_UPSERT_SQL`, `_iv_rank`/`_iv_percentile` and the shared `_RANK_WINDOW_DAYS` trailing window (so index IV-rank is semantically identical to per-underlying IV-rank), behind its own swappable `IndexHistoryProvider`/`set_provider()` seam (distinct from `get_iv()` — index path reads `yfinance.Ticker(...).history()["Close"]`, not the option chain). Config-driven via `config/symbols.yaml` `volatility_indices` (per-entry `ticker`/`symbol`/`ingest`; **VIX kept as `ingest: false`** to record the FRED-lineage decision, not deleted). First run **backfills ~3y** (config `backfill_days`, default 1095) so IV-rank is meaningful immediately; later runs incremental from `max(snapshot_date)`. Honest NULL: holiday/NaN/non-positive → `atm_iv=NULL`, never carried-forward or 0, and excluded from the rank window. No migration (reuses `iv_metrics` from `0002`). Manual run (`python -m etl.sources.vol_indices`). **Curve shape done (#11, 2026-06-16):** `etl/sources/curve_shape.py` — daily front-vs-deferred futures structure → `curve_shape` (idempotent upsert on `(symbol, date)`), **energy-only** (CL/BZ/NG/RB/HO — the underlyings with a free front-month future). Per row: `front_price` (yfinance continuous front `CL=F`), `back_price` (explicit month-coded deferred ~6mo out, e.g. `CLZ26.NYM`), `spread`, `slope_pct` = `((back−front)/front)/(months_out/12)` annualized % carry (positive=contango), `structure` flag (contango/backwardation/`flat` within a config `flat_eps` deadband, default 0.5% annualized). Config-driven via `config/symbols.yaml` `curve` block (front ticker/suffix/deferred root/`months_out`) loaded by `common/config.py` `load_curve_config()`; yfinance fetch behind a swappable `CurveProvider`/`get_curve()`/`set_provider()` seam (only import site). Honest NULL: missing/NaN/stale leg or `front_price<=0` → NULL slope (never ±inf/forward-fill/0). Metals/grains/softs term structure deferred and base-metals (LME) flagged as no free curve — flagged-not-faked. **ETF-roll proxies rejected** (conflate fees/roll methodology with basis). **Realized-front anchoring (#12):** the deferred contract + slope denominator anchor to the front's realized delivery month (config-driven per-contract `front_lead_months`/`roll_day` expiry rules), not the calendar month, so `slope_pct` magnitude stays correctly scaled near rolls (sign was always unaffected); residual ±1-day roll-boundary imprecision degrades to an honest-NULL leg, never a fabricated price. No migration (reuses `curve_shape` from `0002`). Manual run (`python -m etl.sources.curve_shape`). **Phase 3 ETL sources now complete; scheduler wiring still deferred.**
- **Phase 4 — Dashboard (FastAPI):** four panels + macro sub-panel + empty sentiment panel. **STARTED. Foundation + Panel D done (#13, 2026-06-17):** `dashboard/main.py` adds Jinja2 server-side templating (`dashboard/templates/` `base.html` + reusable `_panel.html` shell + `panel_d.html`) and a read-only `GET /panel/d` route; the boot page `/` and `/health` (incl. `schema_version`) are unchanged. Panel D logic lives in `dashboard/panels/panel_d.py` (read-only `SELECT` on `iv_metrics`, no migration): one row per underlying (latest snapshot, `DISTINCT ON (symbol)`), columns `symbol`/`iv_proxy`/`atm_iv`/`iv_rank`/`iv_percentile`/`rv_30`/`iv_rv_spread`/`snapshot_date`, default sort `iv_rank DESC NULLS LAST`. **Conjunctive "rich" highlight:** loud sell-candidate only when `iv_rank≥0.70 AND iv_rv_spread>0`, distinct amber when `iv_rank≥0.70 AND iv_rv_spread≤0` ("RV catching up"), never on rank alone. **Honest cold-start:** rank/percentile NULL until 20 snapshots → render `— (N/20)` (N from a non-null `atm_iv` count, threshold imported from `common.constants._MIN_HISTORY_OBS` — shared single source of truth since #17, never re-hardcoded), with `atm_iv`/`rv_30`/`iv_rv_spread` still shown; off-hours/bad-chain NULL shown as `— (no chain)`, distinct from accruing. **Per-row staleness** badge vs the last expected trading session (weekend/US-holiday-aware table, 2025–26). **GVZ/OVX context strip** at top (level + IV-rank, ~3y-backfilled so meaningful immediately), marked NOT-tradeable / regime read; VIX excluded. DB-down/pre-migration caught (`OperationalError`/`ProgrammingError`) → honest error state, never a 500. New dep `Jinja2==3.1.4`. Tests `tests/test_panel_d.py` (pure-logic + render + live-Postgres-or-skip + error-path). **Panel C done (#14, 2026-06-17):** `dashboard/panels/panel_c.py` + `templates/panel_c.html` at read-only `GET /panel/c` (reads `cot` + `curve_shape`, no migration), mirroring the Panel D foundation. COT table (all ~28 names, latest `report_date` per symbol): headline **3-year COT index** = 156-week percentile of net large-spec (`net_spec = noncomm_long − noncomm_short`, computed at read time over stored history; config `COT_INDEX_LOOKBACK_WEEKS=156`), honest cold-start `— (accruing M/156)` below `COT_MIN_HISTORY_WEEKS=104`, degenerate `max==min` → dash (no NaN/±inf). Loud two-color **crowding flag** (index ≥80 LONG / ≤20 SHORT, named constants) with the directional seller inference rendered in-UI (crowded LONG → tail risk DOWN → don't be short puts/lean calls; crowded SHORT → tail risk UP → don't be short calls/lean puts). Columns: symbol, report_date(+staleness), noncomm_long/short, net_spec(signed), net_spec_pct_oi(%), cot_index_3y, open_interest, inline `structure` echo for the 5 energy names; default sort `ABS(cot_index−50) DESC NULLS LAST`. Separate **energy curve strip** (5 cards CL/BZ/NG/RB/HO, Panel-D-strip pattern): structure flag (loud backwardation), slope_pct, raw front/back/spread, curve date; `structure=NULL` → `— (no curve)`, distinct from `flat`; curve date independent of COT report_date. **COT-correct weekly staleness** (NOT Panel D's daily model): Tuesday data released the following Friday ~15:30 ET; STALE only if stored report_date < latest expected Tuesday whose Friday release passed, with a one-release holiday grace (pure, clock-injectable). In-panel labeling guards: "Legacy COT — non-comm = all large specs (not managed-money)", "index vs each contract's own 3-year range", curve = CURRENT structure (no seasonal adjustment). DB-error isolation (OperationalError/ProgrammingError → honest state, no 500). Tests `tests/test_panel_c.py`. **Panel A done (#15, 2026-06-17):** `dashboard/panels/panel_a.py` + `templates/panel_a.html` at read-only `GET /panel/a` (reads `macro_metrics`, no migration), mirroring the Panel C/D foundation. **Context, not decisions** (Panel D owns the decision). **Five-bucket grouping** (USD / real-rates+breakevens / realized-inflation / growth+labor / risk-regime), config-driven from new additive `freq`+`group` keys on each `config/fred_series.yaml` entry (the FRED ETL ignores unknown keys). **Level PLUS change every row:** daily series (DGS10/DFII10/T10YIE/DTWEXBGS/VIXCLS) ~1m headline + ~3m secondary + neutral arrow; monthly — **inflation indices (CPIAUCSL/PCEPI/PPIACO) headline YoY% computed from the stored 12-mo-prior row**, raw index demoted to secondary (PAYEMS/UNRATE headline MoM level change); quarterly (GDPC1) prior-quarter, marked quarterly. **DFII10 shown directly — never recomputed** as nominal−breakeven (the identity is a footnote only). **Frequency-aware, release-aware staleness** (NOT Panel C weekly / NOT Panel D daily for monthly/quarterly): FRED dates observations by the reference-period START, so monthly/quarterly staleness uses a publication-lag model (`_latest_published_month`/`_latest_published_quarter`, `_MONTHLY_PUB_LAG_DAYS=20`/`_QUARTERLY_PUB_LAG_DAYS=35`) — the freshest released print is never STALE, only a genuinely-missing reference period trips; daily series stay trading-day-aware (`last_expected_session`). **VIX as cross-asset/equity-vol regime, explicitly separated from Panel D's GVZ/OVX** — no IV-rank/percentile on VIX, footnote points to Panel D for commodity vol. Honest NULL (`—`) / missing-prior (`— (no prior)`, never a fabricated 0%); **neutral coloring (no good/bad red/green)**; **no option-action language anywhere** (asserted by a banned-phrase test over rendered output). DTWEXBGS labeled broad-TWI proxy (NOT ICE DXY). DB-error isolation (OperationalError/ProgrammingError → honest state, no 500). Tests `tests/test_panel_a.py`. **Panel A polish (#16, 2026-06-18):** presentation-only follow-up on `panel_a.py`/`panel_a.html` (no migration/source/env): DTWEXBGS ~1m/~3m change rendered as **percent** (not index points) via the existing `pct_change`/`format_pct_change` (rate series stay `pp`, inflation indices unchanged — scoped to DTWEXBGS only); **VIX calm/normal/stressed band tag** from level (`_VIX_CALM_BELOW=15`/`_VIX_STRESSED_ABOVE=25`, NULL→no tag, still no IV-rank on VIX); **USD- and real-rate-trend one-line context clauses** with a commodity-linkage gloss (firming USD → commodity headwind; rising 10y real yield → precious-metals headwind), gated by a **rounding-boundary deadband** (`_USD_TREND_DEADBAND`/`_REAL_RATE_TREND_DEADBAND`) so a sub-rounding drift that displays as `+0.0%`/`+0.00 pp` renders neutral `~flat` instead of a fabricated direction (UAT fix). All #15 invariants preserved (neutral coloring, no option-action language — banned-phrase test extended over band+clauses, honest NULL). **Panel B done (#18, 2026-06-18):** `dashboard/panels/panel_b.py` + `templates/panel_b.html` at read-only `GET /panel/b` (reads `inventories`, no migration), mirroring the Panel A/C/D foundation. **Change-first, not a data dump:** headline = CHANGE (energy weekly build(+)/draw(−) in native unit, NG labeled injection/withdrawal, + Δ-vs-same-week-last-year secondary; grain quarterly YoY %; annual production YoY de-emphasized), absolute level demoted to secondary, deltas computed at read time (EIA revises prior weeks). **Stock-vs-flow enforced:** `PET.WCRFPUS2.W`/`PET.WRPUPUS2.W` tagged FLOW (per-day rate) — never build/draw framing, `percentile_label="n/a (flow)"`. **Honest cold-start:** percentile-in-own-history over **seasonality-aware same-period-of-year comparables** (±10-day window, ~3/yr); below `PANEL_B_MIN_HISTORY_WEEKLY=9` (≈3 prior seasons — a seasonal-**comparable** count, NOT raw weekly obs; fixed from a mislabeled 52 in UAT) / `_QUARTERLY=8` → `— (accruing M/N)` with no verdict, degenerate `max==min` → `—` (no NaN/±inf). **Seasonality:** v1 active mode = raw Δ-vs-same-period-last-year + a LOUD "not seasonally adjusted, NOT the EIA 5-yr band" caveat keyed off `view.seasonality_mode`; the polished same-week-of-year band + 5-yr envelope deferred (flag-not-fake, gated on history depth). **Cadence-bucketed release-aware staleness** (NOT one cross-table rule): weekly EIA Wed (petroleum) / Thu (NG storage) + Monday-holiday +1-day shift + one-release grace (Panel C pattern, current-all-week); quarterly USDA never-red between reports ("as of <quarter>, next ~<date>"); annual production not perma-stale — pure + clock-injectable. Per-series date semantics respected (EIA week-ending / USDA stocks first-of-Sep/Dec/Mar/Jun / production Jan-1 annual). **In-panel neutral directional translation** (tight inventory → supply-tight → upside-tail → vol-bid CONTEXT; decision lives in Panel D) — no option-action language (banned-phrase test over rendered output). Labeled grain-thinness placeholder (3 annual + 3 quarterly only; WASDE + Crop Progress deferred; no stocks-to-use implied). Native units always visible, thousands separators, no `$` on physical quantities, NULL → `—` distinct from 0. DB-error isolation (OperationalError/ProgrammingError → honest state, no 500). Tests `tests/test_panel_b.py`. **Follow-ups filed:** #19 (Cushing tank-bottom proximity context — the one inventory series where the absolute level signals a squeeze/backwardation setup; nice-to-have). **Prices ETL done (#20, 2026-06-18):** `etl/sources/prices.py` — daily yfinance writer → `prices` for the **macro-context trio TLT/VTI/QQQ only** (config-driven off `config/symbols.yaml` `macro_context`; commodity-universe prices deferred — redundant with `curve_shape`/`iv_metrics` and no v1 consumer; rates stay FRED-lineage). **Stores BOTH raw `close` (the tape, stable, broker-checkable) AND back-adjusted total-return `adj_close`** — migration **`0003_prices_adj_close`** adds nullable `adj_close NUMERIC` (the only schema change since `0002`; locally upgrade/downgrade-verified). Fetched with **`auto_adjust=False`** so one call returns raw `Close` + `Adj Close`; non-dividend/unusable-adj bar → `adj_close = close` (uniform convention). OHLCV stored; daily only. First run backfills ~5y (`prices.defaults.backfill_days=1825`); later runs incremental with start floor = **`max(date) − refetch_days`** (`refetch_days=400`) so the ~400-day corporate-action re-fetch keeps `adj_close` internally consistent after each ex-dividend. Idempotent upsert on `(symbol, date)` (`ON CONFLICT ON CONSTRAINT uq_prices_symbol_date` → `DO UPDATE` adj_close + OHLCV); raw `close` expected stable on re-fetch (material >0.1% change logged as a Yahoo correction, never silently clobbered). Honest NULL/skip on empty/NaN/≤0 bar; weekend/holiday → no row (never forward-filled). Behind a swappable `PriceProvider`/`set_provider()` seam (yfinance imported only inside `YFinancePriceProvider`); per-symbol error isolation; `source='yfinance'`. New loaders `common/config.py` `load_macro_context()` + `load_prices_config()`. `yfinance==0.2.66` reused (no bump, no key). Manual run (`python -m etl.sources.prices`); scheduler still deferred. The eventual macro sub-panel renders `adj_close` (label "total-return adjusted") with NYSE-calendar daily staleness. Tests `tests/test_prices.py` (pure logic + provider mocked + live-Postgres-or-skip upsert/idempotency/backfill-floor/holiday/isolation). **Remaining Phase 4: macro-context sub-panel render (now unblocked) + sentiment placeholder.** **Deploy hotfix (#17, 2026-06-18):** the dashboard was crash-looping on Railway (`ModuleNotFoundError: No module named 'etl'`) because `panel_d.py` imported `_MIN_HISTORY_OBS` from `etl.sources.iv`, but the dashboard image ships only `common/`/`config/`/`dashboard/` (no `etl/`, no `yfinance`). Fixed by relocating the constant to new `common/constants.py` (single source of truth, stdlib-only); `etl/sources/iv.py` re-exports it (existing imports unchanged) and `panel_d.py` imports from `common.constants`. The dashboard→etl coupling is removed and statically guarded by `tests/test_panel_d.py::test_no_dashboard_module_imports_etl`. No migration/dep/env/schema change.
- **Phase 5 — Polish & deploy:** deploy Compose stack, release calendar, health checks/logging, redeploy docs.

---

## 7. Repo & workflow

- **GitHub:** `GrapeIsGrape/CommodityDashboard` (the issue-management skills target this repo).
- **Workflow:** ticket-driven — `ba` (spec the ticket) → `implement` (build it) → `close-issue` (verify & close). `enrich-ticket`, `debug`, `list-issues` support the loop.
- **Repository structure:**
  - `docker-compose.yml` — `postgres` / `etl` / `dashboard`, Postgres on a named `pgdata` volume
  - `.env.example` — every env var (DB host/port/name/user/password, `DASHBOARD_PORT`, FRED/EIA/USDA/CFTC key placeholders); `.env` git-ignored
  - `config/symbols.yaml` — v1 commodity universe + commodity→optionable-ETF-proxy mapping + macro-context tickers + `volatility_indices` (GVZ/OVX ingest map with per-entry `ticker`/`symbol`/`ingest` + `backfill_days`; VIX excluded via `ingest: false`) + `curve` block (energy front-vs-deferred map: per-underlying front ticker/exchange suffix/deferred root/`months_out` + per-contract `front_lead_months`/`roll_day` realized-front-expiry rules (#12) + `flat_eps` deadband) + `prices.defaults` block (`backfill_days`/`refetch_days` for the prices ETL #20) — the canonical symbol list
  - `config/fred_series.yaml` — canonical FRED macro series list (id → label/panel + additive `freq`/`group` keys consumed by Panel A #15) + backfill defaults (`observation_start`, `revision_lookback_days`)
  - `config/eia_series.yaml` — canonical EIA energy-inventory series list (legacy series id → label/unit/panel) + backfill defaults; Panel B
  - `config/usda_series.yaml` — canonical USDA NASS QuickStats query list (synthetic id → label/unit/panel + QuickStats `query`) + backfill defaults; Panel B grains
  - `config/cftc_markets.yaml` — canonical CFTC COT market map (symbol → `cftc_contract_market_code`/name) + Socrata dataset & backfill defaults; Panel C
  - `common/config.py` — shared: `get_database_url()` (env→SQLAlchemy URL) + `load_symbols()` + `load_fred_series()` + `load_eia_series()` + `load_usda_series()` + `load_cftc_markets()` + `load_curve_config()` + `load_macro_context()` + `load_prices_config()` (#20)
  - `common/constants.py` — shared cross-service constants (stdlib-only, no source/3rd-party reach-through so it's safe to import from any image): `_MIN_HISTORY_OBS=20`, the IV-rank/percentile accrual threshold consumed by both `etl/sources/iv.py` (re-export) and `dashboard/panels/panel_d.py` (#17 — removed the dashboard→etl import coupling)
  - `dashboard/` — FastAPI app (`main.py`: `/` boot page, `/health` read-only Postgres check that also returns `schema_version` = current Alembic revision, `null` on a pre-migration DB — #8, `GET /panel/d` Panel D — #13, `GET /panel/c` Panel C — #14, `GET /panel/a` Panel A — #15, `GET /panel/b` Panel B — #18), `templates/` (Jinja2: `base.html`, reusable `_panel.html` shell, `panel_d.html`, `panel_c.html`, `panel_a.html`, `panel_b.html`), `panels/` (`panel_d.py` — Panel D view model: conjunctive highlight, accruing `— (N/20)`, daily staleness, GVZ/OVX strip; `panel_c.py` — Panel C view model: 3y COT index + crowding flag, weekly Tue→Fri staleness, energy curve strip; `panel_a.py` — Panel A view model: five-bucket macro grouping, level+change with inflation-YoY, frequency/release-aware staleness, VIX regime; `panel_b.py` — Panel B view model: change-first headline (weekly build/draw + Δ-vs-year-ago, grain YoY), stock-vs-flow tagging, seasonal-comparable cold-start `— (accruing M/N)`, cadence-bucketed release-aware staleness, neutral tight→vol-bid translation; all read-only + DB-error isolation), `Dockerfile`, `requirements.txt` (adds `Jinja2`)
  - `etl/` — `run.py` (entrypoint: applies migrations then idles — no scheduler yet), `sources/` (one module per source: `fred.py` Phase 2 → Panel A, `eia.py` + `usda.py` Phase 2 → Panel B, `cftc.py` Phase 2 → Panel C, `iv.py` + `vol_indices.py` Phase 3 → Panel D (`iv.py` option-chain IV behind a swappable `get_iv()`; `vol_indices.py` GVZ/OVX index levels behind a swappable `IndexHistoryProvider`), `curve_shape.py` Phase 3 → Panel C (front-vs-deferred futures structure behind a swappable `CurveProvider`/`get_curve()`), `prices.py` Phase 4 → macro sub-panel (#20 — daily TLT/VTI/QQQ raw `close` + total-return `adj_close` behind a swappable `PriceProvider`)), `Dockerfile`, `requirements.txt` (adds `requests`, `yfinance`)
  - `migrations/` — Alembic: `alembic.ini`, `env.py`, `script.py.mako`, `versions/0001_baseline.py` (empty baseline), `versions/0002_data_tables.py` (`prices`, `macro_metrics`, `inventories`, `cot`, `iv_metrics`, `curve_shape`, `sentiment_articles`, `sentiment_scores`), `versions/0003_prices_adj_close.py` (adds nullable `adj_close` to `prices` — #20)
  - `tests/` — pytest (`test_config.py`, `test_migrations.py`, `test_fred.py`, `test_eia.py`, `test_usda.py`, `test_cftc.py`, `test_iv.py`, `test_vol_indices.py`, `test_curve_shape.py`, `test_health.py`, `test_panel_d.py`, `test_panel_c.py`, `test_panel_a.py`, `test_panel_b.py`, `test_prices.py` — live-Postgres-or-skip; external APIs/providers mocked)
- **Deployment (Railway):** GitHub repo backs three services — `Postgres` (managed), and `dashboard` + `etl` both built from the **same repo** with Builder=Dockerfile and Dockerfile Path `dashboard/Dockerfile` / `etl/Dockerfile`. Set 5 vars on each code service referencing the DB: `POSTGRES_HOST=${{Postgres.PGHOST}}`, `PORT`/`DB`/`USER`/`PASSWORD` likewise. `dashboard` gets a public domain + healthcheck `/health`; `PORT` is injected by Railway (the dashboard Dockerfile honours `$PORT`). The identical stack also runs locally via `docker compose up` and is portable to Synology — all env-specific config stays in env vars. See README "Deploying to Railway".

> When a change adds a table, migration, ETL source, env var, or service, check whether the relevant section above (§2, §3, §5, §7) is now stale and flag the update — do not let this file drift from the code.
